import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  cardLoader: {
    width: '90%',
    height: 100,
    marginBottom: 10,
    alignSelf: 'center',
  },
  placeholder: {width: '100%', height: '90%', borderRadius: 10},
});
